# Core
streamlit>=1.28.0

# PostgreSQL (for cloud deployment)
psycopg2-binary>=2.9.0

# PDF & Document Processing
pypdf>=3.0.0
docxtpl>=0.11.0
pdf2image>=1.16.0

# PDF Conversion (Windows only - requires Microsoft Word installed)
pywin32>=306; sys_platform == "win32"
docx2pdf>=0.1.8; sys_platform == "win32"

# PDF Generation (works on all platforms - Windows, Linux, Mac)
fpdf2>=2.8.0

# Number to Words
num2words>=0.5.10

# Excel (for auto-update)
openpyxl>=3.1.0